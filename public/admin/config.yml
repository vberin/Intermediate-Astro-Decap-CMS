# Use DecapBridge auth (required)
backend:
  name: git-gateway
  # --- ИЗМЕНЕНИЕ 1: Замените на путь к вашему репозиторию ---
  # Например: BuckyBuck135/astrotiktak
  repo: "ВАШ_ЛОГИН_GITHUB/НАЗВАНИЕ_ВАШЕГО_РЕПОЗИТОРИЯ" 
  branch: main
  # --- ИЗМЕНЕНИЕ 2: Замените на URL, который вы получили с decapbridge.com ---
  identity_url: "https://auth.decapbridge.com/sites/ВСТАВЬТЕ_ВАШ_УНИКАЛЬНЫЙ_ID"
  gateway_url: https://gateway.decapbridge.com

  # Quickly see who did what (optional)
  commit_messages:
    create: Create {{collection}} “{{slug}}” - {{author-name}} <{{author-login}}> via DecapBridge
    update: Update {{collection}} “{{slug}}” - {{author-name}} <{{author-login}}> via DecapBridge
    delete: Delete {{collection}} “{{slug}}” - {{author-name}} <{{author-login}}> via DecapBridge
    uploadMedia: Upload “{{path}}” - {{author-name}} <{{author-login}}> via DecapBridge
    deleteMedia: Delete “{{path}}” - {{author-name}} <{{author-login}}> via DecapBridge
    openAuthoring: Message {{message}} - {{author-name}} <{{author-login}}> via DecapBridge

# Logo for Decap login page. Change url to a link to the image you want to use, no file paths, must be a URL
logo_url: https://decapbridge.com/decapcms-with-bridge.svg

# Add site link in DecapCMS (optional)
site_url: https://astrotiktak.netlify.app # Убедитесь, что здесь URL вашего сайта

media_folder: src/assets/images/blog # Location where files will be stored in the repo - we store them in src so that Astro can optimize them.
public_folder: src/assets/images/blog # Ensure that this path is the same as the media_folder path above.

collections:
  # --- ИСПРАВЛЕНИЕ: Используем 'files' для коллекций, управляющих одним файлом ---
  - name: "content_plan"
    label: "Планировщик Контента"
    files:
      - label: "План публикаций"
        name: "plan"
        file: "src/data/content-plan.json"
        description: "Добавьте сюда темы для будущих статей. Система будет автоматически генерировать и публиковать их в указанную дату."
        widget: "list"
        fields:
          - { label: "Промпт (тема) для статьи", name: "prompt", widget: "string" }
          - { label: "Желаемая дата публикации", name: "publishDate", widget: "datetime" }
          - { label: "Статус", name: "status", widget: "hidden", default: "scheduled" }

  # --- ИСПРАВЛЕНИЕ: Используем 'files' для коллекций, управляющих одним файлом ---
  - name: "authors"
    label: "Авторы"
    files:
     - label: "Список авторов"
       name: "authors_list"
       file: "src/data/authors.json"
       widget: "list"
       fields:
         - { label: "Имя", name: "name", widget: "string" }
         - { label: "Должность", name: "position", widget: "string" }

  - name: "blog" # Used in routes, e.g., /admin/collections/blog. Is also the key when fetching data from the CMS.
    label: "Blog" # Used in the admin dashboard UI
    folder: "src/content/blog" # The path to the folder where the documents are stored
    create: true # Allow users to create new documents in this collection
    fields: # The fields for each document
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Description", name: "description", widget: "string" }
      
      # --- ОБНОВЛЕННОЕ ПОЛЕ "АВТОР" ---
      - label: "Автор"
        name: "author"
        widget: "relation"
        collection: "authors" # Эта связь продолжит работать
        search_fields: ["authors_list.*.name"]
        value_field: "authors_list.*.name"
        display_fields: ["authors_list.*.name"]
        
      - { label: "Date", name: "date", widget: "datetime" }
      - { label: "Cover Image", name: "image", widget: "image" }
      - { label: "Image Caption", name: "imageAlt", widget: "string" }
      - { label: "Is it a featured post?", name: "isFeatured", widget: "boolean", default: false }
      # --- Секция SEO (остается без изменений) ---
      - label: "Тип контента"
        name: "contentType"
        widget: "select"
        options:
          - { label: "Кластерная статья (Обычная)", value: "cluster" }
          - { label: "Страница-Столп (Основная)", value: "pillar" }
        default: "cluster"
      - label: "Родительская страница-столп"
        name: "parentPillar"
        widget: "relation"
        collection: "blog"
        search_fields: ["title"]
        value_field: "{{slug}}"
        display_fields: ["title"]
        required: false
        condition:
          field: "contentType"
          value: "cluster"
      - { label: "Body", name: "body", widget: "markdown" }

